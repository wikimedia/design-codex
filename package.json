{
	"name": "@wikimedia/codex",
	"author": "Wikimedia Foundation",
	"keywords": [
		"Codex",
		"Wikimedia",
		"Vue.js",
		"user-interface",
		"components",
		"Wikipedia",
		"MediaWiki"
	],
	"license": "GPL-2.0+",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"engines": {
		"npm": ">=7.14.0",
		"node": ">=12"
	},
	"scripts": {
		"dev": "npm run build-if-missing -w icons && npm run dev -w vue-components",
		"doc": "npm run build-if-missing -w icons && npm run build -w vitepress",
		"doc:dev": "npm run build-if-missing -w icons && npm run dev -w vitepress",
		"lint": "npm run lint:eslint && npm run lint:stylelint && npm run lint --workspaces --if-present",
		"lint:eslint": "eslint --max-warnings 0 .",
		"lint:stylelint": "stylelint --report-needless-disables --max-warnings 0 .",
		"test": "npm run build-if-missing -w icons && npm run lint && npm run test --workspaces --if-present"
	},
	"devDependencies": {
		"@typescript-eslint/eslint-plugin": "^4.33.0",
		"@typescript-eslint/parser": "^4.33.0",
		"eslint": "^7.32.0",
		"eslint-config-wikimedia": "^0.20.0",
		"eslint-plugin-jest": "^24.5.2",
		"stylelint": "^13.13.1",
		"stylelint-config-wikimedia": "^0.11.1",
		"stylelint-no-unsupported-browser-features": "^5.0.2",
		"stylelint-order": "^4.1.0",
		"typescript": "^4.4.3"
	}
}
