{
	"background-color": {
		"primary": {
			"value": "{ background-color.progressive }"
		},
		"primary--hover": {
			"value": "{ color.modifier-accent50-translucent }"
		},
		"quiet": {
			"value": "{ color.base }"
		},
		"quiet--hover": {
			"value": "{ color.modifier-base90-translucent }"
		},
		"quiet--active": {
			"value": "{ color.modifier-base80-translucent }"
		},
		"quiet-progressive--hover": {
			"value": "{ color.accent90 }"
		},
		"quiet-destructive--hover": {
			"value": "{ color.utility-red90 }"
		},
		"progressive-quiet--hover": {
			"comment": "Deprecated key. Use 'background-color.quiet-progressive--hover' key instead.",
			"value": "{ background-color.quiet-progressive--hover }"
		},
		"input-binary--checked": {
			"value": "{ color.accent50 }"
		},
		"message": {
			"comment": "User system messages (errors, warnings, successes, notices).",
			"notice": {
				"value": "{ color.base80 }"
			},
			"error": {
				"value": "{ color.utility-red90 }"
			},
			"warning": {
				"value": "{ color.utility-yellow90 }"
			},
			"success": {
				"value": "{ color.utility-green90 }"
			}
		},
		"tabs-framed": {
			"comment": "Tabs bar background color.",
			"value": "{ color.base80 }"
		},
		"toolbar": {
			"comment": "Toolbar.",
			"value": "{ background-color.base }"
		},
		"tool--hover": {
			"comment": "Toolbar tools.",
			"value": "{ background-color.base--hover }"
		},
		"tool--active": {
			"value": "{ background-color.progressive }"
		},
		"tool--active-hover": {
			"value": "{ background-color.progressive--hover }"
		}
	},
	"color": {
		"primary": {
			"progressive": {
				"comment": "Primary 'Progressive' Button color and states",
				"value": "{ color.base--inverted }"
			},
			"progressive--hover": {
				"value": "{ color.modifier-accent50-lighten }"
			},
			"progressive--active": {
				"value": "{ color.accent30 }"
			}
		}
	},
	"position": {
		"offset": {
			"comment": "position.offset is a Codex token shorthand to use in `top`, `left`, `bottom`, `right` properties. `offset` as standard property for animating along a path is taken.",
			"input-radio--focus": {
				"comment": "TODO: This has to be a relative value, for design reasons.",
				"value": "-{ size.absolute.25 }"
			}
		}
	},
	"min-size": {
		"icon": {
			"value": "{ size.absolute.125 }"
		}
	},
	"size": {
		"icon": {
			"comment": "TODO: This has to be a relative value, for accessibility reasons.",
			"value": "{ size.absolute.125 }"
		},
		"indicator": {
			"comment": "TODO: This has to be a relative value, for accessibility reasons.",
			"value": "{ size.absolute.75 }"
		}
	},
	"max-width": {
		"button": {
			"comment": "Note, that this is a slight amendment from WikimediaUI Base from `28.75em` = `460px` to `448px`",
			"value": "{ size.absolute.2800 }"
		}
	},
	"margin-top": {
		"options-menu": {
			"value": "-{ border-width.base }"
		}
	},
	"border": {
		"input-binary": {
			"value": "{ border-width.base } { border-style.base } { border-color.input-binary }"
		}
	},
	"border-color": {
		"divider": {
			"value": "{ color.base50 }"
		},
		"primary": {
			"comment": "Deprecated key. Use base file's 'progressive' key instead.",
			"value": "{ border-color.progressive }"
		},
		"primary--hover": {
			"comment": "Deprecated key.",
			"value": "{ border-color.progressive--hover }"
		},
		"primary--active": {
			"comment": "Deprecated key.",
			"value": "{ border-color.progressive--active }"
		},
		"primary--focus": {
			"comment": "Deprecated key.",
			"value": "{ border-color.progressive--focus }"
		},
		"input--hover": {
			"value": "{ border-color.base--active }"
		},
		"input-binary": {
			"value": "{ border-color.base--active }"
		},
		"input-binary--hover": {
			"value": "{ border-color.progressive--hover }"
		},
		"input-binary--active": {
			"value": "{ border-color.progressive--active }"
		},
		"input-binary--focus": {
			"value": "{ border-color.progressive--focus }"
		},
		"input-binary--checked": {
			"value": "{ border-color.progressive }"
		}
	},
	"border-radius": {
		"input-radio": {
			"value": "{ border-radius.circle }"
		}
	},
	"padding": {
		"input-text": {
			"value": "{ padding.vertical.base } { padding.horizontal.input-text }"
		},
		"horizontal": {
			"input-text": {
				"value": "{ size.absolute.50 }"
			}
		},
		"vertical": {
			"menu": {
				"comment": "Note, that we need to rely on `calc()` for cross-stylesheet format output. Not perfectly aligned to current basic browser support.",
				"value": "calc( { padding.vertical.base } + { border-width.base } )"
			}
		}
	},
	"box-shadow": {
		"card": {
			"value": "{ box-shadow.1100 }"
		},
		"dialog": {
			"value": "{ box-shadow.1200 }"
		},
		"menu": {
			"value": "{ box-shadow.1200 }"
		}
	},
	"animation-delay": {
		"progress": {
			"dots": {
				"none": {
					"value": "{ animation-delay.0 }"
				},
				"medium": {
					"value": "{ animation-delay.100 }"
				},
				"slow": {
					"value": "{ animation-delay.200 }"
				}
			}
		}
	},
	"animation-duration": {
		"progress": {
			"dots": {
				"medium": {
					"value": "{ animation-duration.100 }"
				},
				"slow": {
					"value": "{ animation-duration.125 }"
				}
			}
		}
	},
	"transition-property": {
		"icon": {
			"value": "{ transition-property.color }"
		}
	}
}
